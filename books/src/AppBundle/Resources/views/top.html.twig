<nav class="navbar navbar-inverse">
    <div class="container">

        {% if is_granted("IS_AUTHENTICATED_REMEMBERED") %}
            <div class="navbar-header">

                <a class="navbar-brand" href="{{ path('homepage') }}">My library</a>
                <a class="navbar-brand" href="{{ path('books_index') }}">Books</a>
                <a class="navbar-brand" href="{{ path('invitation_index') }}">Invitations</a>
                <a class="navbar-brand" href="{{ path('book_borrow_index') }}">Borrow books</a>

            </div>
            <div class="nav navbar-nav navbar-right">

                <p class="navbar-text">{{ 'layout.logged_in_as'|trans({'%username%': app.user.username}, 'FOSUserBundle') }} </p>
                <a class="navbar-brand" href="{{ path('fos_user_security_logout') }}">
                    {{ 'layout.logout'|trans({}, 'FOSUserBundle') }}
                </a>
            </div>
        {% else %}
            <div class="nav navbar-nav navbar-right">
                <a class="navbar-brand" href="{{ path('fos_user_security_login') }}">{{ 'layout.login'|trans({}, 'FOSUserBundle') }}</a>
                <a class="navbar-brand" href="{{ path('fos_user_registration_register') }}">{{ 'layout.register'|trans({}, 'FOSUserBundle') }}</a>
            </div>
        {% endif %}
    </div>
</nav>